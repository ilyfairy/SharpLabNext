# SharpLabNext Worker Host

This package provides the common ASP.NET Core worker registration, liveness,
readiness, and immutable identity endpoints used by the public language and
artifact worker SDKs.

Use the specialized SDK for a language or artifact processor. This package is
published because it is part of their dependency closure, not as an alternative
Gateway or runtime execution host.
