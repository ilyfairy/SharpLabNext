# Default secret

`internal-service-token` is the public default token used by both Compose files
and generated bundles. Override it with `SHARPLABNEXT_INTERNAL_SERVICE_TOKEN_FILE`
when a different token is required.
